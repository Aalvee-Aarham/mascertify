{
  "name": "mascertify",
  "version": "1.0.0",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "start:server": "cd server && npm start",
    "start:client": "cd client && npm run build && npm run serve",
    "build": "cd client && npm run build && cd ../server && npm install",
    "install:all": "cd server && npm install && cd ../client && npm install",
    "setup": "cp server/.env.example server/.env && echo '✅ Copied .env.example — now add your DB credentials to server/.env'"
  }
}
